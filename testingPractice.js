function sum(a, b) {
  return a + b;
}

function capitalized(str) {
  return str.toUpperCase();
}

function reversedString(str) {
  return str.split("").reverse().join("");
}

export { sum, capitalized, reversedString};

//console.log(reversedString("string"));
